---
import Logo from '@/assets/logo.svg';
import { buttonVariants } from '@/components/ui/button';
import type { MetadataType as Metadata } from '@/components/utils/metadata';
import { paths } from '@/config/paths';
import BaseLayout from '@/layouts/base/index.astro';
import { cn } from '@/lib/shadcn';

const metadata: Metadata = {
  title: '404 - Page Not Found | Better Auth Doc',
  description: 'The page you are looking for could not be found.',
};
---

<BaseLayout metadata={metadata}>
  <main
    class="relative flex h-[calc(100vh-10px-var(--nav-height))] flex-col items-center justify-center"
  >
    <div class="mb-8">
      <Logo class="fill-foreground h-10 w-10" />
    </div>
    <h1 class="text-8xl font-normal">404</h1>
    <p class="mb-8 text-sm">Need help? Visit the docs</p>
    <a
      href={paths.docs.getHref('introduction')}
      class={cn(
        buttonVariants({ size: 'lg' }),
        'px-8 py-3 text-base font-medium',
      )}
    >
      GO TO DOCS
    </a>
  </main>
</BaseLayout>
